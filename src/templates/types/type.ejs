<%_ if (language === 'typescript') { _%>
export interface <%= api.charAt(0).toUpperCase()+api.slice(1) %>Payload {
<%_ for (const field of fields) { _%>
  <%= field.name %>: <%= field.type %>;
<%_ } _%>
}

export interface <%= api.charAt(0).toUpperCase()+api.slice(1) %> extends <%= api.charAt(0).toUpperCase()+api.slice(1) %>Payload {
  id: string;
  createdAt: string;
  updatedAt: string;
}

<%_ } _%>